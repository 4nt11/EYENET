// Mockups run as a pure client-side SPA: adapter-static serves the index.html
// fallback and the client router renders every route (incl. dynamic [category]).
export const ssr = false;
export const prerender = false;
