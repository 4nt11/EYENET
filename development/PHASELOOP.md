# PHASELOOP index — API_PLAN.md driven

One line per phase. The highest-numbered `PHASE-N.md` is the live continuation token.

- [PHASE-1](PHASE-1.md) — ✅ REQUESTED→JOINED membership resolution (post-E5.5 #1 loose end). Atomic CAS-first transition + event/probe detector + self-healing compensation. 1949 passed, 88.28% cov, mypy/ruff clean, 3 adversarial rounds → solid. Checkpoint `546e710`. Next: Group B.
- [PHASE-2](PHASE-2.md) — ✅ M9.B1 file-access signing-key registry + acknowledgment nonces + Ed25519 verify primitives (Group B foundation, §5.5–5.7). User-scoped key lookup + partial-unique single-active enforcement + injection-proof canonical form + atomic nonce CAS. 1972 passed, 88.36% cov, mypy/ruff clean, 2 adversarial rounds (crypto unanimous-solid bar) → solid. Checkpoint `cc1b6c7`.
- [PHASE-3](PHASE-3.md) — ✅ M9.B2 storage core: file_access_journal hash chain + record_access (verify EYENET-SIG-v1 → freshness → atomic nonce-consume + chain-append in one BEGIN IMMEDIATE) + verify_file_access_chain (§5.6). **Decision: CLIENT-SIDE signing** (server verifies only). 1989 passed, 88.45% cov, 2 adversarial rounds (unanimous-solid) → solid.
- [PHASE-5](PHASE-5.md) — ✅ Group B: file-access manifest + access handlers + byte-serving (§5.6). Two-step signed flow: manifest (content_hash+nonce) → POST access (verify EYENET-SIG-v1 + atomic single-use nonce-consume + journal append → stream bytes). NORMAL-tier only; **non-NORMAL fail-closed 403 (tier/clearance gating OUT, deferred PHASE-6)**. 2057 passed, 89.25% cov (≥0.8906 no-drop), surface-pin/mypy/ruff clean, 2 adversarial rounds → solid (closed a nonce-replay MED). Un-merged on `worktree-e55-requested-to-joined`. **Next: PHASE-6 tier/clearance gating + §5.5 503 (isolated authz).**
- [PHASE-4](PHASE-4.md) — ✅ Group B: operator public-key registration (auth boundary). Self-service challenge→PoP→register (`POST /v1/auth/signing-key{/challenge}`), user-bound nonce CAS, **algebraic small-order Ed25519 rejection** (replaced an incomplete byte blocklist that allowed registering an uncontrolled key — proven HIGH bypass, fixed). 2021 passed, 88.43% cov, mypy/ruff/surface-pin clean, 2 adversarial rounds (auth unanimous-solid, crypto re-verified by execution) → solid. Un-merged on `worktree-e55-requested-to-joined`. **Roadmap: PHASE-5 access/manifest+byte-serving (grouped) → PHASE-6 tier-gating (isolated) → PHASE-7 server-key+exoneration §5.8 (isolated) → PHASE-8 anchoring §5.9 (light).**
